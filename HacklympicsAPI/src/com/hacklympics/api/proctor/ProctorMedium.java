package com.hacklympics.api.proctor;

/**
 * A tagging interface which marks the implementing class as
 * a proctor medium (e.g., Snapshot, Keystroke... etc).
 */
public interface ProctorMedium {
    
}